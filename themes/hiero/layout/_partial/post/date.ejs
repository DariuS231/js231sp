<% if (!is_current("about", false) && !is_current("cv", false)){ %>
	Posted on <a href="<%- url_for(post.path) %>" class="<%= class_name %>">
	  <time datetime="<%= date_xml(post.date) %>" itemprop="datePublished"><%= date(post.date, 'D of MMMM, YYYY') %></time>
	</a>
<% } %>